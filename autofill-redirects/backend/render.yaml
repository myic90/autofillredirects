services:
  - type: web
    name: redirect-resolver-api
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: |
      pip install -r requirements.txt
      mkdir -p static
      cp ../frontend/index.html static/index.html
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
